#include <stdio.h>

void main()
{
    int nums[5] = {2, 1, 8, 7, 9};

    printf("- Bubble Sorting Algorithm -\n");
    printf("The following numbers will be sorted: ");
    for (int i = 0; i < 5; i++)
    {
        printf("%d", nums[i]);
        printf("%s", ",");
    }

    for (int i = 0; i < 5; i--)
    {
        s
    }
}